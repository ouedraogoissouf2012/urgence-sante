name: app_foundation
description: "Fondations techniques génériques : environnement, erreurs, résultat."
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # Stockage CHIFFRÉ d'un jeton opaque (Keystore/Keychain), générique et
  # partagé entre applications — même version que patient_mobile.
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
