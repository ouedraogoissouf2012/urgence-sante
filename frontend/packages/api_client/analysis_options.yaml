# Package entièrement GÉNÉRÉ depuis OpenAPI (openapi-generator).
# Le code généré est validé par le générateur ; il n'est pas soumis aux lints
# manuels stricts du workspace. Analyse standard (erreurs réelles) uniquement.
