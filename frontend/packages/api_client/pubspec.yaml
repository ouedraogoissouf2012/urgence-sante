name: api_client
description: "Client REST GÉNÉRÉ depuis docs/api/openapi.yaml — ne pas éditer à la main."
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.4

dependencies:
  collection: '>=1.17.0 <2.0.0'
  http: '>=0.13.0 <2.0.0'
  intl: any
  meta: '>=1.1.8 <2.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
