name: api_client
description: "Client REST généré depuis OpenAPI (issue #7). Placeholder à l'issue #5."
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
