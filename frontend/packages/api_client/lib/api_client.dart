/// Client REST de l'API Urgence Santé.
///
/// Ce package hébergera le client **généré depuis OpenAPI** (ADR-004), isolé
/// dans un sous-dossier `generated/`. Il est volontairement minimal à l'issue
/// #5 et sera peuplé à l'issue #7.
library;

/// Version du contrat d'API ciblé (informative, en attendant la génération).
const String apiContractVersion = 'v1';
