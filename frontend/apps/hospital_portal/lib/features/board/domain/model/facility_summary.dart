/// Établissement sélectionnable par l'agent (accès démo, sans authentification).
class FacilitySummary {
  const FacilitySummary({required this.id, required this.name});

  final String id;
  final String name;
}
