/**
 * Briques transverses partagées par les modules métier.
 *
 * <p>Ce package regroupe des primitives neutres et sans métier : type résultat,
 * erreurs de base, identifiants typés, horloge injectable, pagination. Il ne
 * dépend d'aucun module métier et ne contient aucune règle de domaine.
 */
package com.urgencesante.buildingblocks;
